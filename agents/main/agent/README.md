# `main` agent folder

This directory holds [`models.json`](models.json) synced from the root [`openclaw.json`](../../../openclaw.json) Ollama catalog.

There is **no** `main` entry in [`openclaw.json`](../../../openclaw.json) → `agents.list`. It is not a live Discord-bound agent like `jake`, `joy`, `harlan`, or `hep`. Keep this folder only if something external references it; otherwise it can be removed.
