# TOOLS.md — HEP

Notes for this coding agent’s environment (not shared with Jake’s workspace copy).

## Project paths

- Mission Control dashboard: `/Users/Jack/.openclaw/workspace/jake-mission-control`
- OpenClaw home: `/Users/Jack/.openclaw`

## Conventions

- Resolve paths explicitly; this workspace is not the app repo root.  
- Skills may still load from `~/.openclaw/skills` and the main workspace `skills/` when configured.

Add SSH hosts, package managers, or stack-specific commands here as needed.
