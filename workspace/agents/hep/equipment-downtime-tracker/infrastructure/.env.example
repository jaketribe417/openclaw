# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=edt_admin
DB_PASSWORD=changeme_in_production
DB_NAME=equipment_downtime_tracker
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# MinIO Configuration
MINIO_HOST=localhost
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme_in_production
MINIO_BUCKET_FLOOR_MAPS=floor-maps
MINIO_BUCKET_EQUIPMENT_PHOTOS=equipment-photos
MINIO_BUCKET_ATTACHMENTS=operator-attachments
MINIO_ENDPOINT=http://${MINIO_HOST}:${MINIO_PORT}
MINIO_USE_SSL=false

# Application Configuration
APP_PORT=3001
NODE_ENV=development
JWT_SECRET=your_jwt_secret_here_change_in_production
JWT_EXPIRES_IN=24h

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001
